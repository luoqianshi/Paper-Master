# 骨架变体摘录卡

> 标准骨架：`研究动机 → 文章贡献 → 本文方法 → 实验结果 → 优点和创新点`（占 42%）。以下 4 张卡覆盖另外 4 类高频变体，摘录均原样取自博客园作者「乌漆 WhiteMoon」的论文阅读笔记，未做改写、未做翻译。

## 变体1：survey 变体（研究问题列表 + 经验总结回环）
- **来源**：`回归/06-resampling-strategies-imbalanced-regression-survey.md`
- **触发条件**：论文是综述或带大规模实证比较的文章，开篇抛出一串可判定的研究问题，结尾需要逐条给出可落地的经验结论。
- **H2 序列**：`不平衡回归问题 → 本文目标和贡献 → 基本概念和分类方法 → 重采样算法 → 实验研究（内含「经验总结」回环）`

> ## 本文目标和贡献
> 本文的主要目的是从不同的角度来分析重采样策略在处理不平衡回归问题的效果，采用了不同的重采样策略和学习算法进行了广泛的实验研究，还使用了不同的不平衡回归任务的性能指标。
> **本文从以下研究问题进行切入：**
>
> 1. 是否值得使用重采样策略？
> 2. 哪些重采样策略对预测性能的影响最大？
> 3. 最佳策略的选择是否取决于问题、学习模式和使用的指标？
> 4. 每种策略产生的训练实例的数量是否会影响结果？
> ……（下略）
>
> ## 重采样算法
> 处理不平衡数据集的最常见方法是通过重采样改变数据分布以平衡目标值，主要包括欠采样、过采样和混合采样。本文忽略了没有公开源码、没有良好的可复现性、不频繁使用的研究，对广泛使用的不平衡回归重采样算法进行了归纳和分析。
>
> ## 经验总结
> 1. 是否值得使用重采样策略：对于使用的两个评估指标，使用的四种重采样方法比不使用重采样获得更优性能的次数更多，通过 Nemenyi 事后检验可见应用重采样方法在统计上性能更好，因此使用（某些）重采样策略是有利的。
> 2. 哪种重采样方法对预测性能影响最大：在 F1 指标上 RO 和 GN 对模型起到了积极影响，SERA 指标上 GN 和 WERCS 的性能最佳。……（下略）
>
> 附表格表头（「不平衡回归算法」节）：`| 论文 | 发表年份 | 核心思想 |`

- **这个变体的关键动作**：
  1. 在「本文目标和贡献」里用编号列表**逐字照抄**论文提出的 N 条研究问题，不要改写成散文。
  2. 在「实验研究」末尾开一个「经验总结」小节，条目与研究问题一一对应、逐条回答，形成首尾回环。
  3. 每一类方法单独占一个 H2/H3，节内用「论文 | 发表年份 | 核心思想」表格归纳，只摘表头行 + 代表性几行，不整表搬运。

## 变体2：survey 变体（分类体系靠标题层级承载，以「分析与展望」收尾）
- **来源**：`神经决策模型/04-survey-neural-trees-co-evolving-neural.md`
- **触发条件**：综述提出了一套自己的分类体系，三类（或若干类）方法各自成体系，需要让读者只看标题就能背下分类树。
- **H2 序列**：`神经树的研究动机 → 非混合：神经网络与决策树协作方法 → 半混合：利用类别层次结构的神经网络 → 混合：神经决策树 → 分析与展望`

> ## 神经树的研究动机
> 神经网络（NNs）和决策树（DTs）是机器学习中两种流行但范式迥异的模型，它们具有**互斥的优势和局限性**。神经网络和决策树的优势与局限如下表所示：
> `| 机器学习模型 | 优势 | 局限 |`
>
> ## 混合：神经决策树
> **神经决策树 NDTs** 是神经网络与决策树完全融合的产物，其核心理念是构建一个**兼具神经网络强大表示学习能力和决策树透明推理过程**的混合模型。与事后解释黑箱模型的“非混合”方法不同，NDTs 设计初衷就是**内在可解释**的。其关键创新在于**利用可微的神经网络组件来实现决策树中的路由函数**，从而使得整个树结构可以通过梯度下降进行端到端的全局优化，克服了传统决策树贪心、不可微的缺点。NDTs 可根据是否实现**类别层次结构**进行分类：……（下略）
>
> ## 分析与展望
> 评估模型可解释性有两个关键标准：
>
> 1. **可分解性**：模型能否被分解为功能明确、可独立理解的组件（如决策树的节点和路径）。决策树是高度可分解的，而典型的神经网络则不然。
> 2. **透明度**：模型的决策过程是否清晰可循、易于理解（如决策树基于信息增益的分裂规则）。神经网络的参数编码决策逻辑，缺乏透明度。……（下略）

- **这个变体的关键动作**：
  1. 用 H2 承载分类的第一层（非混合 / 半混合 / 混合），H3 承载分类的第二层（各技术路线），让分类体系直接长在标题层级上。
  2. 每个 H2 标题写成「<类别名>：<一句话界定>」，禁止用「相关工作」「主流方法」这类无信息量的标题；每类末尾放一个「小结」H3 收口。
  3. 结尾固定用「分析与展望」收束：先给评估标准，再逐组件给经验结论，最后列 3 条未来方向。

## 变体3：方法名替换 H2 变体
- **来源**：`进化计算/01-evolutionary-forest-regression.md`
- **触发条件**：论文提出的方法有一个明确的、可命名的方法名（如「进化森林算法」），用方法名做标题比「本文方法」更利于检索与记忆。
- **H2 序列**：`研究动机 → 文章贡献 → 进化森林算法 → 实验分析 → 优点和创新点`

> ## 研究动机
> 基于树的集成学习方法的置信区间通常很大，这表明这些算法的实验数据方差较高。一种可能的解释是，这些方法都在原始特征空间或线性变换的特征空间上拟合训练数据，因此这些算法可能难以充分描述自变量和因变量之间的关系。……（下略）
>
> ## 文章贡献
> 文章提出了一种进化森林算法，它将多组非线性特征组合成一个倾斜的随机森林。本算法中使用了遗传规划（GP）方法来构造非线性特征，每个GP个体代表一组特征。为了获得最优的随机森林，算法在外部存储了进化过程中各种表现良好的 GP 个体。在进化结束时，根据存储的 GP 个体建立一组决策树，形成最终的集成模型。
>
> ## 进化森林算法（### 整体框架）
> EF 算法的种群中，每个个体都是一组GP树用于构建多个特征。评估GP个体时先用这些特征训练决策树，然后使用5折交叉验证方案来计算其适应度值，表现良好的GP个体被另外存储，在进化过程结束时形成目标进化森林。

- **这个变体的关键动作**：
  1. 把「本文方法」这个 H2 换成论文方法的具体名称（如「进化森林算法」），标题中不出现「本文」二字。
  2. 方法节内部按算法组件切分 H3（整体框架 / 个体设计 / 交叉变异 / 适应度评估 / 选择算子 / 精英保留），一节只讲一个组件。
  3. 实验节改叫「实验分析」，H3 按验证维度切分（特征构建比较 / 算法性能比较 / 参数设置的影响 / 算法组件的选择 / 算法对比），一个 H3 只回答一个问题。

## 变体4：双变体（预备知识 + 理论分析 两个 H2 都占）
- **来源**：`决策树/09-oblique-decision-trees-derivatives-relu.md`
- **触发条件**：论文含成体系的数学结论（定理 / 引理 / 复杂度分析），且读懂方法前必须先补齐符号与定义；理论本身是主要卖点之一。
- **H2 序列**：`研究动机 → 文章贡献 → 预备知识 → 本文方法 → 理论分析 → 实验结果 → 优点和创新点`

> ## 预备知识
> 由于任何不平衡树都可以通过添加虚拟决策节点（如 $0^{\top}x \ge -1$）变为平衡树，因此可以定义深度为 $T$ 的平衡斜决策树函数类如下：
>
> 1. **根节点决策**：$r_1 \triangleq I[\omega_{\varnothing}^{\top}x + \beta_{\varnothing} \geq 0]$，其中 $\omega_{\varnothing} \in \mathbb{R}^D, \beta_{\varnothing} \in \mathbb{R}$ 是根节点的权重和偏置。
> 2. **递归决策**：对于 $i \in \{2,3,\ldots,T\}$……（下略）
>
> ## 理论分析（### 表示等价性）
> 本文在数学上证明了 LCN 的函数类与斜决策树的函数类是等价的。`#### 定理 2`：该定理内容为任何斜决策树都可以表示为一个规范架构的局部常数网络 $g(\tilde{o}^M)$。证明：对于一个深度为 $T$ 的斜决策树，它有 $2^T - 1$ 个决策权重和偏置。我们可以构造一个 LCN，其 $M=1$（仅一层），但该层有 $N_1 = 2^T - 1$ 个神经元。……（下略）
>
> ## 实验结果（### 数据集和实验设置）
> 使用在 MoleculeNet 的化学性质预测数据集上进行评估，使用了 4 个多标签二分类数据集 Bace, HIV, SIDER, Tox21，以及 1 个回归数据集 PDBbind。……（下略）
> 附表格表头：`| 算法类型 | 对比模型 |`、`| 算法类型 | 调参策略 |`

- **这个变体的关键动作**：
  1. 在「文章贡献」之后、「本文方法」之前插入「预备知识」H2，只写读懂方法必需的符号与定义，不展开证明。
  2. 在「本文方法」之后、「实验结果」之前插入「理论分析」H2，用「表示等价性 / 结构化的共享参数化」这类 H3 分组，单个定理用 H4 承载，只写「定理内容 + 证明思路」。
  3. 实验节先给数据集与设置的对比表（表头行 + 关键几行），再分「对比实验 / 消融实验」；把「为什么有效」留给「理论分析」。
