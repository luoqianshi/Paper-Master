# batch-B09（决策树 03-08，「决策树可微化/梯度下降」主题集群）

### 第一段：CSV 区

```
file|skeleton_variant|deviation_reason|h3_logic|motivation_pattern|contribution_form|formula_style|table_types|pros_themes|transferable
03-tree-decision-trees-decision-graphs.md|标准|-|混合|综述式铺陈|散文整段|三段式完整|无表|可解释性/计算效率|Y，节点共享+「先定义损失再增长结构」↔检测特征复用与结构搜索；全篇公式皆为截图须改 LaTeX
04-pctbagging-inner-ensembles-trade-off-between.md|预备知识插入|贡献与方法之间插入 H2「合并树构建」补 CTC 前置算法，本文方法仍保留|按模块|综述式铺陈|散文整段|无公式|无表|可解释性/实验充分|Y，96 数据集 Friedman+贝叶斯符号秩检验↔多数据集 mAP 显著性检验；单一超参数连续调节性能-可解释权衡↔检测精度-速度档位权衡
05-vanilla-gradient-descent-oblique-decision.md|标准|-|按模块|两列表（现有方法|局限性）|散文整段|三段式完整|组件对应表|理论统一等价/可微端到端|Y，语义等价定理正反证明+组件对应表+分类/回归/RL 三任务并列↔检测多任务并列与「模块等价性」论证
06-gradtree-learning-axis-aligned-decision-trees.md|预备知识插入|方法前插 H2「预备知识」，下设 Entmax/Hardmax 两个 H3 补激活函数|按流程步骤|编号列表逐个点评|散文整段|三段式完整|符号表+方法对比表|可微端到端/可解释性|Y，C1/C2/C3 编号障碍逐个击破↔检测小目标/遮挡/尺度逐条破解；ST entmax 消融↔换模块消融
07-bivariate-decision-trees-smaller-interpretable.md|标准|-|按定义-结构-变体|编号列表逐个点评|散文整段|三段式完整|无表|稀疏结构/可解释性|Y，三分裂方案+正则化路径/相位图↔检测多尺度分支的成本-收益选择；特征成本正则化↔参数量与 GFLOPs 约束
08-symbolic-regression-enhanced-decision-trees.md|标准|-|无H3|综述式铺陈|散文整段|三段式完整|无表|可解释性/鲁棒泛化|Y，合成集→真实集→噪声鲁棒→可解释分析四段实验↔检测合成/消融/mAP/可视化四段；SR 搜索本身不迁移
```

> 骨架：标准 4/6（03、05、07、08），预备知识插入 2/6（04、06），**方法名替换H2 0/6**。贡献节 6/6 散文整段。表格：仅 05、06 有表，其余 4 篇全图/无表（本批表格密度显著低于全语料均值）。

### 第二段：PHRASE 区

**动机首句**(6/6)：定调句「XX 是／具有……（地位／优势／应用）」——05「决策树（DTs）作为高度非线性的机器学习模型，在表格数据等领域表现出色」、06「决策树模型因其高度可解释性而备受青睐」、07「决策树在统计机器学习中具有独特地位」、03「DT 和树集成在实践中得到了广泛的应用」、04「分类是机器学习中非常常见的问题」、08「传统决策树在表格分类任务中虽然具有出色的可解释性优势」。

**动机转折句**(9)：「然而」4 次+「但是／但」5 次——06「然而，这种"步步为营"的策略存在弱点：」、07「但是决策树的缺点是预测准确性较低，这主要源于两个原因：」、03「但是 DT 存在两个缺点」、05「然而学习准确的决策树，特别是斜决策树时经常存在组合爆炸问题」。4/6 转折后接「（主要有）两个／以下原因：」再展开编号列表。

**贡献首句**(6/6)「本文提出（了）……」——4/6 以「针对……（问题／缺点）」状语起头：04「针对 CTC 分类性能较差和 Bagging 的可解释性较差的问题，本文提出了一种结合 CTC 和 Bagging 的算法 PCTBagging」、03「针对决策树模型的缺点，受 Network in Network 和斜决策树的启发，本文提出树中树(TnT)算法」；06、07 用「一种名为／一个」。

**公式铺垫句**(10)：「如下公式所示。」7 次（03×3、06×2、07×2）为最高频；「定义如下」3 次；「其公式定义为：」(06 Hardmax)；「XX 的通用形式由以下优化问题定义：」(06 Entmax)；「本文定义如下最小化损失函数。」(03)；「数学表示为 X = [...]」(05)、「其计算过程表示为：valueₓ(I) = A·x + b」(05)。

**「其中」解释句**(26)：符号表档「其中相关的符号含义如下：」(06) + `| 符号 | 含义 |`；行内档 06「其中 $\iota$ 是在某个分割处考虑的特征的索引，$\tau$ 是对应的阈值」、07「其中 $L(\cdot,\cdot)$ 是 0/1 损失函数，$\lambda$ 是正则化超参数」、08「其中 $t$ 为当前节点，$c$ 为类别数」、03「其中 #Xsubset,i 是节点 ni 处子集的样本抽样数」。

**图注衔接句**：「下图展示了……」7 次、「下图显示了……」5 次、「如下图所示」6 次、「下表显示了……」4 次、「结果如下表所示」3 次。分布极不均匀：**04 独占 16 次「下图」**（「下图展示了 CTC 的构建过程」「下图显示了 Friedman 检验确定的差异情况」），**06 全篇 0 次**，改用「如下公式所示／如伪代码所示」贴公式图。

**优点条目**(6)：①「本文首次将硬斜决策树通过可逆编码映射为神经网络结构，实现决策树与神经网络的严格语义等价，为梯度下降直接优化决策树奠定了理论基础」②「模型设计中尽量减少了 STE 等梯度近似方法的使用，支持使用标准梯度下降进行训练，显著提升了优化精度和训练稳定性」③「提出了稠密决策树表示法，将离散的树参数转化为连续可微的矩阵形式」④「引入特征成本正则化项，使节点能根据数据特性自适应选择零变量（剪枝）、单变量或双变量分裂，实现真正的结构学习」⑤「TnT 不依赖于预定义的图结构或树结构，而是在定义了损失函数的基础上通过对决策树的节点递归地进行增长、合并实现，生成决策结构的方式很新颖」⑥「实验部分作者应用了大量的假设检验方法，如果后续做同样的实验可以参考本文的做法和表述」。

### 【差异化·本批专项】

**① 05 与 06 的同主题差异化 + 互链取证**
- 动机段：05 用四列对比表「方法类型|代表算法|优势|局限性」一次铺开四类学树范式，再点名 DGT/ICCT 的 STE 缺陷；06 改为「贪心算法两条弱点编号列表」+ 三行「非贪心决策树方法|不足」表，且把「可解释性」设为评判标尺（斜树因牺牲可解释性被列进不足）。
- 方法段：05 按「结构→样例→定理证明→任务变体」四块平铺，核心是**等价性证明**；06 按「公式描述→稠密表示→反向传播」流程推进，并把障碍编号 C1/C2/C3，用「#### 针对 C1/C2/C3」三个 H4 逐个击破。同一主题，一篇走**结构论证**、一篇走**问题-解法编号**，骨架不撞车。
- 互链：本批 03-08 **正文互链 0 处**，彼此不引用、不重复背景，靠「同一背景压缩到 1 段 + 各自换论证主轴」去重合。全语料唯一互链原句在集外：`决策森林/11` L38「可以先看 GradTree 的原文，**也可以看我发的另一篇博客 [GradTree: Learning Axis-Aligned Decision Trees with Gradient Descent](…)**」。另有一类**隐性互链**：后出笔记把前序方法名收进对比表——`决策树/10` L165「| **梯度优化树** | GradTree, DGT, DTSemNet, SoftDT, LatentTree |」、L34「如 GradTree 在原文中报告其在 36 个数据集的 17 个上表现不如 CART」。

**② 「方法名替换 H2」的 if-then 判据**（全语料 15 篇无「## 本文方法」，其中约 11 篇为方法名/机制短语替换）
- IF 方法只有**单一主线**、且所有 H3 子节都能被同一个方法名统摄 → THEN 方法名上提为 H2（例：`特征工程/03`「## eXplainable Reasonably Randomised Forest」、`进化计算/01`「## 进化森林算法」、`神经决策模型/02`「## 自适应神经树」）。
- IF 方法段需拆成 **2 个以上 H2**（方法本体 + 优化/求解/扩展）→ THEN 各段用方法名或机制短语命名（例：`决策树/01`「Nonlinear Decision Tree」+「双层优化方法」、`决策树/10`「斜决策树训练的无约束优化重构」+「基于梯度的整树优化之可微决策树训练」）。
- IF 方法节下出现**兄弟方案并存**或**无法被方法名统摄的节**（定理证明、多任务扩展、通用组件）→ THEN **退回「## 本文方法」**，把方法名下压到 H3。本批正是此型：07 有 Bivariate TAO / bivariate CART 两条兄弟路径；05 的 H3 含「语义等价性定理及其证明」「具体任务的实现」，均非 DTSemNet 专属子模块；03 的 TnT 名只挂 H3「### Tree in Tree」。
- 反证：方法名有辨识度（DTSemNet、TnT 都是论文自造名）**不是**触发条件——05、03 名字够响仍保留「本文方法」。真正的触发变量是「方法名能否统摄全部子节 / 是否需要多个 H2」。
- 附加：前置背景需独立成节时插 H2（04「## 合并树构建」、06「## 预备知识」），但**不替换**「## 本文方法」。

**③ 05 号标准骨架基准校准（逐节首句 / H3 / 公式）**
- H2 序列：研究动机 → 文章贡献 → 本文方法 → 实验结果 → 优点和创新点（5 节，无增删）。
- 动机：定调句 + 「然而……」+ 四列表 + 「为了……大多数先前工作采用……。但是……同时……也存在缺陷，如……」收尾（先立表、后点名具体算法缺陷）。
- 贡献：单段散文，句序为「命名 → 机制（通过……实现……）→ 可微性/等价性 → 模型结构走查（输入层……第一隐藏层……第二隐藏层……输出层……）→ 首次性主张 → 实验与意义」。
- 本文方法 4 个 H3：①「### DTSemNet 结构」首句「对于一棵非叶节点的决策函数表示为"A·x + b > 0 ?"的倾斜决策树，DTSemNet 能够将其语义等价地编码为神经网络。」→ 组件对应表 → 逐层说明 → 「权重值由以下三条规则决定：」三条编号规则 → 「通过以下定理确保……」→ 4 步前向流程编号列表。②「### 模型结构样例」首句「下图展示了一个 DTSemNet 的模型结构样例，以及和等价的倾斜决策树结构：」→ 第二张对应表 → 4 步路径编码 → 第三张表「决策节点|路径关系|应用规则|连接权重」→ 行内公式与不等式推导。③「### 语义等价性定理及其证明」首句「定理 1（语义等价性）：考虑将……即对于所有输入向量 x∈X，有：」→ 公式图 → 「证明基于以下重要观察：」3 条 → 「- 正向证明：……采用反证法：……」「- 反向证明：……」。④「### 具体任务的实现」用三个「在……（多分类/回归/强化学习）方面，……」并列段，每段配一图。
- 实验 4 个 H3 按任务分：分类/回归/强化学习性能比较 + 敏感性分析；每节首句模板「在 N 个 XX 数据集上对比了……，结果如下表所示，可见……」，末句补一句归因（「回归性能的差异主要源于梯度近似策略的不同」）。
- 公式处理：**公式几乎全部为截图**，仅少量行内 LaTeX；三段式由「铺垫句 + 图/行内式 + 规则列表或『其中』解释」三者拼成，作者不做符号表，改用「组件对应表」承担符号释义功能。
