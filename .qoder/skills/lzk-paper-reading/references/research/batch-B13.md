# Batch B13｜回归 01-03 + 进化计算 01-02（SMOTER / SMOGN / DenseWeight / 进化森林 / PS-Tree）

## CSV

file|skeleton_variant|deviation_reason|h3_logic|motivation_pattern|contribution_form|formula_style|table_types|pros_themes|transferable
---|---|---|---|---|---|---|---|---|---
回归/01-smote-regression|尾部缺失|无「优点和创新点」节，实验节末图后即收尾（2013 会议短文）|按模块（relevance／欠采样／SMOTE 变体）|综述式铺陈（分类三类解法→回归侧空缺）|散文整段|无公式（仅行内文本定义 Dr={<x,y>∈D:φ(y)≥t}）|无表|重采样不平衡|Y：重采样范式可平移为检测中的类别不平衡与小目标/难例过采样；F1 换成 mAP 与 PR 曲线
回归/02-smogn|尾部缺失|同批最短篇，无优点节；实验节另加「pre 和 recall 的分析」H3|无H3（方法节整段走查伪代码；实验节 H3 按模块）|综述式铺陈（分类成熟→回归/数据流空缺）|散文整段|无公式|无表|重采样不平衡|Y：过采样+安全距离判据类比难例挖掘与样本平衡；pre-recall 权衡段换成精度-召回分析
回归/03-density-based-weighting|标准|-（唯实验节深至 H4）|按模块（DenseWeight／稀有度度量／权重函数／DenseLoss），实验节 H3→H4 双层|综述式铺陈（重采样 vs 成本敏感对比递进）|散文整段|三段式完整（公式(1)~(6) 全配铺垫+「其中」）|无表|重采样不平衡+可解释性|Y：损失加权/成本敏感范式可直接套 YOLO 的 Focal/Wise-IoU 类重加权；「单超参 α 权衡常见-稀有」即长尾检测权重
进化计算/01-evolutionary-forest|方法名替换H2|「本文方法」→「进化森林算法」、「实验结果」→「实验分析」；方法节未被拆，仍单 H2 下开 6 个 H3|按流程步骤（框架→个体设计→交叉变异→适应度评估→选择算子→精英保留）|综述式铺陈（方差大→特征空间→GP→集成，四步递进）|其他（先散文述法，再「文章的主要贡献有：」列 2 条）|无公式|无表|特征工程+计算效率|Y：骨架直接套 YOLO 改进文（个体设计→Backbone/Neck/Head，47 倍加速→FPS/Params/GFLOPs），117 数据集多视角实验即多尺度对比
进化计算/02-ps-tree|方法名替换H2|「本文方法」→「分段符号回归树」、「实验结果」→「实验分析」；H3 下再开 H4，共 4 级标题|混合（H3 按定义-结构：个体表示／算法流程；H4 按流程步骤与模块混合）|综述式铺陈（黑盒/白盒两分法，逐个点评 CART、PL-Tree、GP 并配拟合曲线图）|散文整段|三段式完整（公式后接「其中…」或「式子中的符号和含义如下表所示」+符号表）|符号表+组件对应表|可解释性+实验充分|Y：自适应分区类比尺度/区域自适应与动态标签分配；「精度-模型大小-训练时间」三目标即 mAP-Params-FPS

备注：标准骨架仅 1/5。① **新增变异「尾部缺失」**（回归 01/02 短文省掉优点节，与论文分量相关）。② **方法名替换 H2 在本批 2/5 且成对替换**（方法节+实验节同时改名），与 B12 的 03-xrrf 同型，是以方法名做顶层标签的稳定亚型。③ **回归连续指标处理**：不裸用 MSE/RMSE，统一「目标值分箱→按样本数排序为 bin rank 1~5」离散化，配归一化 MAE×归一化密度 p'(y) 的移动平均平滑曲线、临界差值图；句式仍用「结果可见……优于……」但必带量化口径（「340 种情况中 57.6% 明显优于，仅 3.2% 变差」）。**未出现**预测值-真实值散点图/残差图。④ **无 LaTeX**：5 篇共 84 张图、零内联公式，公式全部截图并用「如公式(N)所示」编号指回；图后解释仅两式——「其中 XX 是/表示 YY」与「式子中的符号和含义如下表所示」+符号表。除 PS-Tree 独占 5 张表外，其余 4 篇除论文概况表外零表。

## PHRASE

**动机首句**（「XX 是……的问题/方法，……」出现 5 次）
- 「目前已经有各种各样的回归方法被提出，从可解释性的角度可以将这些方法分为黑盒方法和白盒方法。」 (02-ps-tree)
- 「不平衡学习是一个主要在分类任务上研究的问题，并已经取得了一定的研究成果。」 (02-smogn)

**动机转折句**（「但是/然而」出现 21 次）
- 「然而由于连续和离散目标值之间的固有差异，这些方法不能直接应用于回归任务。」 (03)
- 「但是它们本质上是分段常数函数。」 ／「但是也因其低解释性而被诟病。」 (02-ps-tree)
- 「但是这类算法可能会直接抛弃不利于训练的特征，而这些特征不见得同样不利于测试集。」 (进化森林)
- 递进套句「一个可能的解决方案是……考虑到这个问题，一个方法是……」 (进化森林，2 次)

**贡献首句**（「针对 XX 问题，本文提出了 YY」2 次；「本文/文章提出了一种……」3 次）
- 「针对罕见极值预测问题研究的空缺，本文首次提出了针对该问题的重采样方法。」 (01)
- 「综合 CART 和 GP 方法的优点，本文提出了一种新的回归方法——分段符号回归树(PS-Tree)。」 (02-ps-tree)

**公式铺垫句**（「如公式(N)所示」5 次；「可以[将 X] 表示为/定义为下面的式子」6 次）
- 「KDE 如公式(1)所示」「归一化如公式(2)所示，其中 p(Y) 是……」「基本的权重函数使用公式(3)进行定义」 (03)
- 「可以将优化目标表述为下面的公式」「缩放个体大小的公式如下：」「可以将目标函数定义为如下的式子：」 (02-ps-tree)

**公式后的「其中」解释句**（全批「其中」12 次，公式后专用 2 次）
- 「其中 p(Y) 是使用密度函数 p 对 Y 的计算结果。」 (03)
- 「其中 pk 是一个权重向量，表示每个数据点属于分区 k 的概率。」 (02-ps-tree)
- 「式子中的符号和含义如下表所示」+符号表 (02-ps-tree，2 次)——符号多则以表代句

**图注衔接句**（「下图展示/显示/给出……」14 次；「可见/结果表明/可以看出/说明」33 次）
- 「下图展示了使用不同 α 设置的模型的不同 bin 的在数据集上的平均 RMSE 值，可见……」 (03)
- 「下图为四个数据集的训练和测试曲线，可以看出……」 (02-ps-tree)
- 「下图展示了特征选择过程之前和之后的特征，可见 Lasso 将特征数从 26 个减少到 15 个，性能只降低了很小程度」 (02-ps-tree)
- 反例：进化森林 19 张图**零衔接句**，图直接贴段末

**优点条目（6 条）**（引导句「个人认为，本文有如下一些优点和创新点可供参考学习：」3 次）
1. GP 中的个体都只有两层，然后通过集成多个 GP 树来保证构建的新特征具有多样性，在提高性能的同时仍有不错的拟合效果； (进化森林)
2. 实验数据丰富有力，在 117 个数据集上进行了实验，并从 7 个方面评价本文算法的优势，说服力强。 (进化森林)
3. PS-Tree 结合了分类树、非线性回归和 GP，几种算法强烈耦合、缺一不可，都为问题的求解提供了正面效益，非常有创意； (02-ps-tree)
4. 为了避免在适应度评估阶段带来的巨大时空开销，本文将评估问题转换成了一个多目标优化问题，从而能得到个体的适应度； (02-ps-tree)
5. DenseLoss 仅有一个超参数 α，只需要对 α 进行调参即可适应不同的不平衡情况，较为简洁。 (03)

补记（PS-Tree）：公式树不写 LaTeX，靠结构图（30 张）+ 伪代码符号表 + 步骤表三层替代；方法段把优化目标**显式重写两次**（MSE → 特征重要性 |∂fk/∂Φi| → 追加个体大小目标）。
